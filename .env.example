# Keep provider credentials on the local/server side. Never prefix this key with VITE_.
GEMINI_API_KEY=PASTE_YOUR_GEMINI_API_KEY_HERE
GEMINI_MODEL=gemini-3.1-flash-lite

# Optional: point the browser at a deployed server endpoint with the same contract.
# VITE_PERSONALIZATION_ENDPOINT=/api/personalize
